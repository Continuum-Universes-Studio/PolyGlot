name: Filesystem MCP
version: 0.0.1
schema: v1

mcpServers:
  - name: Filesystem
    type: stdio
    command: npx
    args:
      - -y
      - "@modelcontextprotocol/server-filesystem"
      - "/mnt/y/Dev/Softwares/PolyGlot"
